// MIT © 2017 azu
// import { Engine } from "prh";
import { Dictionary } from "../../domain/Dictionary";

export function convertDictionaryToJSON(dictionary: Dictionary) {
    // const engine = new Engine({
    //     rules: [{
    //         expected: dictionary.expects,
    //         patterns: dictionary.pattern,
    //     }]
    // });
    // engine.replaceByRule()
}
